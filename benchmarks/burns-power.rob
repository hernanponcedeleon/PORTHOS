Thread 0
While (True)
  a <- 1;
  x := a;
  sync;
  chk <- y;
  While (chk != 0)
    chk <- y;
  lwsync;
  a <- 0;
  x := a

Thread 1
While (True)
  chk <- x;
  While (chk != 0)
    chk <- x;
  lwsync;
  b <- 1;
  y := b;
  sync;
  chk <- x;
  b <- 0;
  if (chk == 0)
    y := b
  else
    y := b
