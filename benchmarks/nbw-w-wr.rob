Thread 0
cnt <- CCF;
a0 <- (cnt + 1);
CCF := a0;
a1 <- 666;
BUFF := a1;
a2 <- (cnt + 2);
CCF := a2

Thread 1
cnt <- CCF;
b0 <- (cnt + 1);
CCF := b0;
b1 <- 666;
BUFF := b1;
b2 <- (cnt + 2);
CCF := b2;
cnt_b <- CCF;
value <- BUFF;
cnt_e <- CCF;
While (cnt_b != cnt_e or (cnt_b % 2) == 1)
  cnt_b <- CCF;
  value <- BUFF;
  cnt_e <- CCF
