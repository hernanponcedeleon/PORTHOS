Thread 0
cnt <- CCF;
a <- (cnt + 1);
CCF := a;
a <- 666;
BUFF := a;
a <- (cnt + 2);
CCF := a

Thread 1
cnt_b <- CCF;
cnt_e <- CCF;
While (cnt_b != cnt_e or (cnt_b % 2) == 1)
  cnt_b <- CCF;
  value <- BUFF;
  cnt_e <- CCF
